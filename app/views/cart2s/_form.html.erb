<%= form_for(@cart2) do |f| %>
  <% if @cart2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cart2.errors.count, "error") %> prohibited this cart2 from being saved:</h2>

      <ul>
      <% @cart2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
